/** @type {import('next').NextConfig} */
const nextConfig = {
  // Explicit production mode; Render sets PORT — next start reads it automatically.
  reactStrictMode: true,
};

export default nextConfig;
